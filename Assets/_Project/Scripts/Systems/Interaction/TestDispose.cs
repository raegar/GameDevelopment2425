using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestDispose : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("TestTree");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
